let name = "anas ber "
let count=0

for(let i=0;i<name.length;i++){
    if(name[i]=="a" || name[i]=="e" || name[i]=="u" || name[i]=="i" || name[i]=="o"){
        count++
    }
}
console.log(count);
